#:schema https://yazi-rs.github.io/schemas/theme.json

# Yazi keeps its shipped file and directory icons; only the adaptive palette
# is selected here.
[flavor]
dark = "sunbather-dark"
light = "sunbather-light"
